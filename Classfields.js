import * as React from 'react';
import { View, StyleSheet, Dimensions, Text} from 'react-native';

export default class Classfields extends React.Component {
  state = {
  };

  render() {
    return (
      <View>
        <Text>Classfields</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({
  scene: {
    flex: 1,
  },
});
